extends layouts/main
include mixins/videoBlock

block contentBlock
    .home-videos
        each item in videos
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.videoFile
            })
            
            
            