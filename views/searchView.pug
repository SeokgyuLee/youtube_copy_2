extends layouts/main
include mixins/videoBlock
block contentBlock
    .search__header
        h3 search for: #{searchingBy}
    .search__videos
        each item in videos
                +videoBlock({
                    title:item.title,
                    views:item.views,
                    videoFile:item.videoFile
                })