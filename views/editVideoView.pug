extends layouts/main

block contentBlock
    p editVideo! by Pug
    .form-contatiner
        form(action=`videos/${mRoutes.editVideo}`, method="post")
        input(type="text", placeholder = "write title", name = "title")
        textarea(name="description", placeholder="Description")
        input(type="submit", value = "Update Video")
    a.form-container__link.form-container__link--delete(href=`videos/${mRoutes.deleteVideo`}) Delete Video